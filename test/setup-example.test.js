// unit testing
// tdd (test-driven development)
describe('Availability', () => {
  describe('.authenticate()', () => {
    it('accepts a username/password pair')

    it('returns a promise')

    it('resolves with a JWT token')
  })

  describe('.createUser', () => {
    it('this')

    it('that')
  })
})
